version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: formfit-frontend
    ports:
      - "5173:5173"
    restart: unless-stopped
